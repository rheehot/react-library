import * as React from "react";
import Layout from "../layout/Layout";

const Home = () => {
		return (
				// @ts-ignore
				<Layout>
						hello world<br/>
				</Layout>
		);
};

export default Home;