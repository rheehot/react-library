import * as React from "react";
import "./Layout.scss";

// @ts-ignore
const Layout = ({children}) => {

		return (
				<>
						<header>a</header>
						<section>{children}</section>
						<footer>a</footer>
				</>
		);
};

export default Layout;