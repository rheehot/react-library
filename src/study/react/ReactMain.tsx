import * as React from "react";

export default class ReactMain extends React.Component {

    render() {
        return (
            <div>
                React Main화면입니다.
            </div>
        );
    }
};
