1 * 9 = ?
ㅁㅁㅁㅁㅁㅁㅁ[제출]
결과 : 정답입니다 or 오답입니다.

계산기컴포넌트 입장)
1. 문제필요. 화면에 뿌려야댐.
2. 확인버튼 눌렀을 때 입력값을 줄태니 정답인지 여부만 알려주셈.
3. 오답이면 인풋박스에 포커스.

구구단 계산기 만드는 입장)
function1. makeQuiz(): Quiz
function2. isAnswer(quiz: Quiz)

advance)
정답 캐싱. 문제를 만드는 순간 정답은 딱 하나로 정해지는 경우,
매번 답안지 제출했을 때마다 연산을 할 수는 없음.

하지만 문제를 만드는 순간 정답이 하나로 정해지지 않고,
사용자가 제출한 답에 따라 정답이 다른 경우에는 정답캐싱 기능이 없도록 유연하게 구현해야함.


====> 설계 실패1.

Quiz DTO부터가 실패했음.
makeQuiz()는 사실 필요한건 문제 하나임.
그 안에 뭐 이거저거 더 있긴한데 그건 제출할 때 필요한거지.

그래서 문제가 됬던게 바로 입력다해서 제출할 때였음.
state안에 quizDTO있고, (ㅡㅡ 심지어 state에 필요한건 또 문제 하나였음. 나머지는 화면그릴일 없잖아?)
제출할 때 입력한걸 처음 quizDTO안에 넣어야하는데 이게 state안에 있어서 setState로 넣어야하는데,
setState는 비동기로 작동하기 때문에 제가 제출한 걸 토대로 정답을 판별해줄지 안해줄지를 모른다는거임.
