Tree
    TicTacToeHomeV2
    ㄴ TicTacToeGame
    ㄴ TicTacToeCell

[TicTacToeHomeV2]
prop: x
state: x

[TicTacToeGame]
prop: squareCount, playerList
state: cellGrid, message
field: currentPlayerIndex
methods: mark
shouldComponentUpdate: x PureComponent로 적용.

[TicTacToeCell]
prop: cellValue, rowIndex, columnIndex, mark
state:
shouldComponentUpdate: cellValue가 바뀌었을 때만 다시 렌더링
